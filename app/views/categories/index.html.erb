<h1>Here are all of the categories for your recipes!</h1>

<ul>
  <% @categories.each do |category| %>
    <li><%= category.name %></li>
    <%= link_to "delete", category_path(category), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this?"}%>
  <% end %>
</ul>

<%= link_to "add a new category", new_category_path %>
<%# link_to is the same as an a tag %>
<%# category_path is the prefix we are linking to and passing in the category id using the correct category from the iteration %>
