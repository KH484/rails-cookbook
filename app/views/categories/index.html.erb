<h1>Here are all of the categories for your recipes!</h1>

<ul>
  <% @categories.each do |category| %>
    <li><%= category.name %></li>
    <%= link_to "delete", category_path(category), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this?"}%>
  <% end %>
</ul>
